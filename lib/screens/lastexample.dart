import 'package:flutter/material.dart';
import 'package:http/http.dart' as http;

class Last_example extends StatefulWidget {
  const Last_example({Key? key}) : super(key: key);

  @override
  State<Last_example> createState() => _Last_exampleState();
}

class _Last_exampleState extends State<Last_example> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Api Course Last Example"),
      ),
      body: Column(
        children: [],
      ),
    );
  }
}
